name: Update QuickJS

on:
  - push
  - pull_request

jobs:
  update-quickjs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v3
        with:
          go-version: 'stable'
      - run: ./scripts/update-qjs
      - run: go test -v ./...
